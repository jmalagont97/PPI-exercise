### [2026-09-21 01:06] - Diseño del dataset del goal 01: positivos directos, negativos Pool A balanceados, self-interacciones retenidas
- **Principal Investigator Proposal (User):** El primer goal se orienta a la obtención de datos: (1) recuperar las secuencias de las proteínas implicadas, (2) generar pares positivos = únicamente interacciones directas (MI:0407), y (3) generar pares negativos aleatorios del Pool A (pares entre las 8,777 proteínas del interactoma sin NINGUNA evidencia de interacción en el archivo maestro). El dataset se mantiene balanceado: misma cantidad de negativos que de positivos. NO se eliminan las self-interacciones (78 pares A–A), ya que la arquitectura siamesa con cabezas no compartidas permite intercambios entre las ramas.
- **Co-Investigator Analysis (AI under active role):** Pool A confirma distribución de "proteínas estudiadas" (mitiga sesgo de anotación del Pool B); espacio disponible 99.89% de los 38.5M pares posibles, riesgo de colisión con evidencia trivial de filtrar. Balance 1:1 evita sesgo de clase mayoritaria. Self-interacciones: con ESM-2 ambas ramas reciben el mismo embedding base, pero cabezas independientes (sin pesos compartidos) proyectan de forma distinta — el par (A,A) es bien definido y aporta señal sobre homodimerización; se retienen las 78 self-interacciones directas → positivos totales: 1,424 pares únicos MI:0407 (incluidas self-interacciones).
- **Consensus:** Positivos = 1,424 pares únicos MI:0407 (self-interacciones incluidas). Negativos = pares aleatorios del Pool A sin ninguna evidencia, cantidad igual a los positivos (1,424). Secuencias vía UniProt REST para todas las proteínas involucradas. Decisión metodológica registrada: self-interacciones NO se eliminan por compatibilidad con cabezas no compartidas.

### [2026-09-21 02:30] - Diseño de exp_02: recuperación de secuencias desde UniProt
- **Proposal (User):** Usar los IDs UniProt únicos disponibles en `data/ground_truth.tsv`, descargar desde UniProt las secuencias de aminoácidos correspondientes y almacenarlas en un archivo FASTA conservando el mismo ID del archivo de entrada.
- **Analysis (AI under active role):** El experimento se define como una recuperación y auditoría de datos: la entrada son las columnas `uniprot_A` y `uniprot_B`; la salida debe conservar literalmente las cabeceras UniProt y reportar cualquier ID no recuperado.
- **Consensus:** Aprobado el diseño de `exp_02` para recuperar las secuencias desde UniProt y producir un FASTA trazable, con cobertura completa y ausencia de cabeceras duplicadas como criterios de éxito.

### [2026-09-21 02:00] - Diseño de exp_03: filtrado del ground truth por disponibilidad de secuencias
- **Proposal (User):** Cruzar las secuencias disponibles en `goals/01/experiments/exp_02/results/sequences.fasta` con `goals/01/experiments/exp_01/results/ground_truth.tsv` y eliminar toda interacción positiva o negativa que involucre una proteína sin secuencia.
- **Analysis (AI under active role):** La unidad de filtrado es la interacción completa: una fila se conserva sólo cuando ambos IDs, `uniprot_A` y `uniprot_B`, aparecen en el FASTA con una secuencia no vacía. Se preservan el esquema y los metadatos de las filas conservadas, y se auditan las filas eliminadas y el balance de etiquetas resultante.
- **Consensus:** Aprobado `exp_03` con regla de intersección estricta de secuencias en ambos extremos; no se eliminan filas por su etiqueta, sino por ausencia de representación secuencial de cualquiera de sus participantes.

### [2026-09-21 02:12] - Diseño de exp_04: embeddings de proteínas con ESM2-650M
- **Proposal (User):** Usar `Synthyra/ESM2-650M` para extraer la representación de todas las proteínas del FASTA, guardar un HDF5 con el ID de proteína y su embedding, y evaluar la viabilidad considerando las longitudes de las proteínas y mean pooling sin incluir tokens especiales.
- **Analysis (AI under active role):** El checkpoint tiene dimensión oculta 1280 y un límite práctico de aproximadamente 1024 residuos biológicos por pasada debido a los tokens especiales. Para este ejercicio académico no se realizará procesamiento por ventanas ni truncamiento: se excluirán del experimento las proteínas que superen el tamaño máximo permitido por el modelo. El embedding será la media de las representaciones de los residuos biológicos, excluyendo BOS, EOS, padding y cualquier token especial.
- **Consensus:** Aprobado el diseño de `exp_04`: procesar únicamente proteínas con longitud compatible con el límite del modelo, omitir las proteínas más largas y almacenar un embedding de dimensión 1280 por ID en un archivo HDF5. La salida debe conservar la correspondencia ID–embedding, la longitud de la secuencia y la identificación de las proteínas omitidas; no se inicia todavía la implementación ni la ejecución.

### [2026-09-21 02:15] - Revisión de exp_03: filtro de longitud para pool compatible con ESM2-650M
- **Proposal (User):** Incorporar directamente en `exp_03` el filtro de longitud, sobreescribiendo sus artefactos, para que el único pool de entrada a la extracción de embeddings contenga proteínas de máximo 1024 residuos. Las proteínas más largas se ignoran por tratarse de un ejercicio académico.
- **Analysis (AI under active role):** Se amplió la regla de intersección de secuencias: cada interacción se conserva sólo si ambos IDs están disponibles y ambas secuencias tienen longitud `<= 1024`. Se mantienen auditorías por fila, motivos de eliminación, longitudes, hashes de entrada y el esquema original del ground truth.
- **Consensus:** `exp_03` queda como el experimento de preparación definitivo para embeddings; no se creará un filtro separado en `exp_04`. La extracción ESM2 utilizará `ground_truth_filtered.tsv` y el conjunto de proteínas elegibles.

### [2026-09-21 02:50] - Cierre del goal 01
- **Principal Investigator Proposal (User):** Dar por terminado el goal 01 tras completar la preparación del dataset, recuperar las secuencias compatibles y generar los embeddings ESM2 en HDF5; promover los tres artefactos principales a `data/`.
- **Co-Investigator Analysis (AI under active role):** El objetivo se cumple: `exp_01`–`exp_04` están completados; `exp_03` deja 2.233 interacciones y 2.769 proteínas elegibles; `exp_04` genera un embedding de dimensión 1.280 por proteína, con IDs y longitudes alineados, pooling medio sobre residuos biológicos y exclusión de tokens especiales. Los artefactos promovidos son enlaces simbólicos relativos, por lo que el almacén compartido conserva trazabilidad hacia cada experimento.
- **Consensus:** Goal 01 cerrado. Los artefactos compartidos definitivos son `data/ground_truth_filtered.tsv`, `data/sequences.fasta` y `data/embeddings.h5`. El siguiente trabajo podrá consumirlos para el modelado secuencia–secuencia sin repetir las etapas de preparación ni extracción.