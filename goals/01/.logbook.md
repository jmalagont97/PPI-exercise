# Logbook — goal 01

## [2026-09-21 02:10] - Experiment Consolidation: exp_01 — success
- **Target Milestone:** Generar el archivo maestro de anotaciones `ground_truth.tsv`: 1,424 positivos (MI:0407, self-incluidas) + 1,424 negativos Pool A, balance 1:1, seed 42, cero colisiones.
- **Hypothesis & Metric Validation:** Hipótesis soportada ✅ — 2,848 filas generadas; auditoría independiente contra el MITAB maestro: 0 colisiones negativo-evidencia, 0 duplicados, 78 self-loops presentes, cobertura locus AGI 98.1% (27 pares positivos con locus faltante conservados). Fuente: sha256 ae19f5b90f7d…; script commit f5c96b9; version.txt con seed+hash+commit.
- **Consolidated Artifacts:** `experiments/exp_01/results/ground_truth.tsv`, `experiments/exp_01/results/version.txt`, `experiments/exp_01/reports/summary.md`
- **Actionable Takeaways:** 3,086 proteínas únicas en el GT dimensionan la recuperación de secuencias (exp_02). Nota operativa: `conda activate bioDL` falla por bug del plugin anaconda_anon_usage (conda 26.3.2) — usar el binario directo `~/miniconda3/envs/bioDL/bin/python` (también en lanzamientos tmux). Negativos del Pool A con sesgo de "ausencia de evidencia" aceptado por consenso; validación contra Pool B opcional a futuro.

## [2026-09-21 06:55] - Experiment Consolidation: exp_02 — success
- **Target Milestone:** Recuperar las secuencias de aminoácidos UniProt para los IDs únicos de `data/ground_truth.tsv`, conservando los IDs originales en las cabeceras FASTA.
- **Hypothesis & Metric Validation:** Se recuperaron 2,893 de 3,086 IDs únicos (**93.7459% de cobertura**); el FASTA tiene 2,893 cabeceras únicas, todas pertenecientes al conjunto de entrada. Quedaron 193 IDs documentados en `missing_ids.tsv`/`audit.tsv` (100 con error HTTP y 93 no devueltos). La cobertura no alcanza el criterio estricto de 100% del diseño, pero se acepta como cobertura operativa deseable por decisión del PI; no se corrige ni repite el script.
- **Consolidated Artifacts:** `experiments/exp_02/results/sequences.fasta`, `experiments/exp_02/results/audit.tsv`, `experiments/exp_02/results/missing_ids.tsv`, `experiments/exp_02/results/version.txt`. Entrada: SHA-256 `2166a1a77d6dd0679ffca4fb2dff44a095f73bc3bd1bf8cd958ff3598b16a3aa`; commit `6a225a6356b7d581ffee62813e334e40d5b8b0e6`.
- **Actionable Takeaways:** El FASTA puede utilizarse como conjunto principal de secuencias; los 193 IDs faltantes deben excluirse o tratarse explícitamente en las etapas posteriores. La recuperación de secuencias está terminada y no requiere cambios adicionales.

## [2026-09-21 02:01] - Experiment Consolidation: exp_03 — success
- **Target Milestone:** Filtrar `ground_truth.tsv` conservando únicamente interacciones cuyos dos participantes tienen una secuencia no vacía en `sequences.fasta`.
- **Hypothesis & Metric Validation:** Hipótesis soportada ✅ — de 2,848 interacciones, se conservaron 2,442 y se eliminaron 406. La auditoría confirma que todas las filas conservadas tienen secuencia disponible para A y B, y que toda fila eliminada tiene al menos un participante sin secuencia. La distribución final es 1,201 positivas y 1,241 negativas.
- **Consolidated Artifacts:** `experiments/exp_03/results/ground_truth_filtered.tsv`, `experiments/exp_03/results/filter_audit.tsv`, `experiments/exp_03/results/removed_interactions.tsv`, `experiments/exp_03/results/summary.tsv`, `experiments/exp_03/results/version.txt`.
- **Actionable Takeaways:** El conjunto filtrado es apto para la siguiente etapa de modelado secuencia-secuencia; las 406 interacciones retiradas permanecen trazables por fila original y motivo, sin modificar los artefactos de `exp_01` ni `exp_02`.

## [2026-09-21 02:15] - Experiment Consolidation: exp_03 — success (revisión con longitud máxima)
- **Target Milestone:** Sobreescribir el pool de `exp_03` para que todas las interacciones conservadas sean compatibles con la extracción ESM2-650M usando un máximo de 1.024 residuos por proteína.
- **Hypothesis & Metric Validation:** Hipótesis soportada ✅ — de 2.848 interacciones se conservaron 2.233 y se eliminaron 615. El FASTA contiene 2.893 proteínas disponibles; 2.769 cumplen el límite y 124 fueron excluidas por longitud. La auditoría independiente confirmó que todas las filas conservadas tienen ambos participantes disponibles y con longitud `<= 1024`. La distribución final es 1.156 negativas y 1.077 positivas.
- **Consolidated Artifacts:** `experiments/exp_03/results/ground_truth_filtered.tsv`, `experiments/exp_03/results/filter_audit.tsv`, `experiments/exp_03/results/removed_interactions.tsv`, `experiments/exp_03/results/summary.tsv`, `experiments/exp_03/results/version.txt`, `experiments/exp_03/scripts/filter_ground_truth_by_sequence.py`.
- **Actionable Takeaways:** `ground_truth_filtered.tsv` queda como pool definitivo para extraer embeddings; no se implementarán ventanas ni truncamiento. Las exclusiones por secuencia ausente o longitud permanecen trazables por fila y motivo. `exp_01` y `exp_02` no fueron modificados.

## [2026-09-21 02:25] - Experiment Consolidation: exp_04 — failure
- **Target Milestone:** Lanzar la extracción ESM2-650M en CUDA y generar el HDF5 de embeddings.
- **Failure Postmortem:** La corrida no llegó a ejecutar `extract_esm2_embeddings.py`. El comando de tmux intentó `conda activate bioDL`, pero `conda 26.3.2` falló dentro del plugin `anaconda_anon_usage` con `TypeError: expected str, bytes or os.PathLike object, not NoneType` al resolver `old_conda_prefix` en `_get_deactivate_scripts`. No se generó ningún resultado y no hay evidencia de error del modelo ni del pooling.
- **Consolidated Artifacts:** No se generaron artefactos de extracción. Diagnóstico reproducido: `/home/jmalagont/miniconda3/envs/bioDL/bin/python` existe y puede importar PyTorch 2.5.1+cu121, Transformers 4.46.3 y detectar CUDA; `h5py` está instalado sólo en base, no en `bioDL`.
- **Actionable Takeaways:** No modificar el código por este error. Corregir primero el lanzamiento del entorno usando el intérprete directo de `bioDL` y añadir `h5py` a ese entorno; después repetir la validación de dependencias antes de relanzar.

## [2026-09-21 02:29] - Experiment Consolidation: exp_04 — failure
- **Target Milestone:** Cargar `Synthyra/ESM2-650M` e iniciar la extracción de embeddings.
- **Failure Postmortem:** El entorno ya quedó operativo y `h5py` está disponible en `bioDL`, pero `AutoModel.from_pretrained("Synthyra/ESM2-650M")` solicitó confirmación interactiva para ejecutar custom code del repositorio. Como la corrida se ejecutó sin stdin interactivo, Transformers abortó con `ValueError` indicando que debe pasarse `trust_remote_code=True`. La extracción no procesó ninguna proteína y no produjo HDF5.
- **Consolidated Artifacts:** No se generaron resultados. El fallo se localiza en la carga del modelo, en `extract_esm2_embeddings.py:158`; el tokenizer se cargó correctamente.
- **Actionable Takeaways:** No modificar todavía el código. La corrección mínima pendiente es autorizar explícitamente el custom code del modelo en las llamadas de carga pertinentes, después de revisar el alcance de esa autorización.

## [2026-09-21 02:31] - Experiment Consolidation: exp_04 — failure
- **Target Milestone:** Cargar el modelo personalizado y comenzar la extracción en CUDA.
- **Failure Postmortem:** Tras añadir `trust_remote_code=True`, la carga avanzó hasta `modeling_fastplms.py`, pero falló porque `transformers` 4.46.3 no exporta `AttentionInterface` ni `AttentionMaskInterface`, símbolos requeridos por el custom code. No se procesó ninguna proteína y no se generó HDF5.
- **Consolidated Artifacts:** No se generaron resultados. El intérprete de `bioDL` confirma Python 3.10.20, PyTorch 2.5.1+cu121 y Transformers 4.46.3; ambos símbolos están ausentes. La documentación actual visible del modelo declara Python 3.11–3.14, PyTorch 2.13 y Transformers 5.13.
- **Actionable Takeaways:** No cambiar pooling ni lógica de extracción. Resolver primero la incompatibilidad de runtime: usar un entorno compatible con la versión actual del modelo o fijar una revisión/modelo compatible con el entorno existente.

## [2026-09-21 02:45] - Experiment Consolidation: exp_04 — success
- **Target Milestone:** Extraer una representación ESM2 de cada proteína elegible y conservar la correspondencia ID–embedding en HDF5.
- **Hypothesis & Metric Validation:** Éxito. Se procesaron 2.769 de 2.893 proteínas; 124 fueron excluidas por longitud >1024; 0 fallos. El HDF5 tiene forma `(2769, 1280)`, IDs en el mismo orden que el FASTA elegible, longitudes congruentes, valores finitos y pooling `mean_residue_only` con tokens especiales excluidos. Tiempo: 314.572 s; throughput: 8.802 proteínas/s.
- **Consolidated Artifacts:** `goals/01/experiments/exp_04/results/embeddings.h5`, `embedding_audit.tsv`, `summary.tsv` y `version.txt`.
- **Actionable Takeaways:** La extracción es congruente y usable para el modelado posterior. El validador automático contiene un falso positivo al comprobar el atributo booleano `special_tokens_excluded` mediante identidad (`is not True`); la comprobación independiente con igualdad confirma que el atributo es verdadero. Este defecto queda pendiente de corrección separada.

## [2026-09-21 02:50] - Cierre del goal 01 — success
- **Target Milestone:** Cerrar la línea de investigación del goal 01 con los datos de interacción filtrados, el FASTA compatible y los embeddings ESM2 promovidos al almacén compartido.
- **Hypothesis & Metric Validation:** Goal completado. `exp_01`, `exp_02`, `exp_03` y `exp_04` están en estado `completed`. El pool definitivo contiene 2.233 interacciones; el FASTA contiene 2.893 proteínas recuperadas; 2.769 proteínas de longitud `<=1024` fueron embebidas y 124 fueron excluidas. El HDF5 tiene forma `(2769, 1280)`, correspondencia ID–embedding exacta, longitudes correctas, valores finitos, mean pooling sobre residuos biológicos y exclusión de tokens especiales.
- **Consolidated Artifacts:** Enlaces simbólicos relativos en `data/`: `ground_truth_filtered.tsv`, `sequences.fasta` y `embeddings.h5`, apuntando a los resultados definitivos de `exp_03`, `exp_02` y `exp_04`, respectivamente.
- **Actionable Takeaways:** Goal 01 cerrado. El siguiente goal puede iniciar el modelado secuencia–secuencia consumiendo directamente los tres artefactos de `data/`. El validador de embeddings conserva un falso positivo conocido en la comparación del atributo booleano, sin afectar la validez del HDF5.